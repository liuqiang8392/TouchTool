# TouchTool 点击助手

[![License](https://img.shields.io/badge/License-GPL%20v3-blue.svg)](LICENSE)

TouchTool 是一款 Android 自动化点击工具，通过无障碍服务实现自动点击、屏幕录制、OCR 识别等功能，帮助用户实现手机操作的自动化。

## 核心特色

### 蓝图式任务编辑
TouchTool 采用直观的**蓝图式任务编辑器**，通过可视化节点连接方式创建自动化流程。用户可以像搭建积木一样，将各种自动化动作以节点形式拖拽到画布上，并通过连接线定义执行顺序和逻辑关系，无需编写代码即可完成复杂的自动化任务设计。

### 多种多样的自动化动作
应用提供了**200+ 种自动化动作**，覆盖系统操作、界面交互、数据处理、逻辑控制等多个维度，满足各种自动化需求。

## 自动化动作分类

### 触发器类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `手动触发` | 手动启动任务执行 |
| `应用启动触发` | 监听指定应用启动时触发任务 |
| `应用退出触发` | 监听指定应用退出时触发任务 |
| `定时触发` | 在指定时间自动执行任务 |
| `通知触发` | 监听系统通知时触发任务 |
| `网络状态触发` | 监听网络状态变化时触发任务 |
| `电池状态触发` | 监听电池状态变化时触发任务 |
| `屏幕状态触发` | 监听屏幕开启/关闭时触发任务 |
| `蓝牙设备触发` | 监听蓝牙设备连接时触发任务 |
| `分享接收触发` | 接收到分享内容时触发任务 |
| `广播接收触发` | 接收系统广播时触发任务 |

### 逻辑控制类动作
| 动作类型              | 功能说明        |
|-------------------|-------------|
| `条件判断 (IF)`       | 根据条件执行不同分支  |
| `等待条件 (WAIT IF)`  | 等待条件满足后继续执行 |
| `分支选择 (SWITCH)`   | 多条件分支选择     |
| `手动选择 (CHOICE)`   | 多选项选择执行     |
| `循环 (FOR)`        | 指定次数循环执行    |
| `条件循环 (WHILE)`    | 条件满足时循环执行   |
| `顺序执行 (SEQUENCE)` | 按顺序执行多个动作   |
| `随机执行 (RANDOM)`   | 随机选择执行分支    |
| `并行执行 (PARALLEL)` | 多个分支并行执行    |

### 系统操作类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `Shell命令` | 执行系统 Shell 命令 |
| `分享内容` | 将内容分享到其他应用 |
| `系统按键` | 模拟系统按键操作 |
| `播放铃声` | 播放系统提示音 |
| `文字转语音` | 将文字转换为语音播放 |
| `振动` | 控制设备振动 |
| `发送通知` | 发送系统通知 |
| `发送广播` | 发送系统广播 |
| `发送Toast` | 显示 Toast 提示 |
| `剪贴板操作` | 读取/写入剪贴板内容 |
| `屏幕控制` | 开启/关闭屏幕 |
| `截屏控制` | 开启/关闭截屏功能 |
| `获取电池状态` | 获取当前电池状态 |
| `获取网络状态` | 获取当前网络连接状态 |
| `获取WiFi信息` | 获取当前 WiFi 连接信息 |
| `获取蓝牙设备` | 获取已连接的蓝牙设备 |
| `获取日期时间` | 获取当前日期和时间 |
| `音量控制` | 获取/设置设备音量 |

### 界面交互类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `查找控件` | 在界面中查找指定控件 |
| `控件点击` | 点击指定控件 |
| `文本输入` | 在输入框中输入文本 |
| `粘贴内容` | 在输入框中粘贴内容 |
| `控件选择` | 选择控件中的选项 |
| `获取控件信息` | 获取控件的文本、位置等信息 |
| `获取子控件` | 获取控件的子元素 |
| `获取父控件` | 获取控件的父元素 |
| `获取窗口` | 获取当前窗口信息 |
| `触摸操作` | 模拟触摸、滑动等手势 |
| `多点触控` | 模拟多点触控操作 |

### 图像处理类动作
| 动作类型 | 功能说明 |
|--------|---------|
| `截取屏幕图像` | 获取当前屏幕截图 |
| `加载图片` | 从文件加载图片 |
| `裁剪图片` | 裁剪图片指定区域 |
| `缩放图片` | 调整图片尺寸 |
| `保存图片` | 将图片保存到文件 |
| `查找图片` | 在屏幕中查找指定图片 |
| `查找多个图片` | 在屏幕中查找多个匹配图片 |
| `图片存在检测` | 检测指定图片是否存在于屏幕 |
| `点击图片` | 点击屏幕上找到的图片位置 |
| `生成二维码` | 生成二维码图片 |
| `解析二维码` | 解析二维码图片内容 |
| `目标检测` | 使用 YOLO 模型进行目标检测 |

### 颜色处理类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `获取颜色` | 获取屏幕上指定位置的颜色 |
| `查找颜色` | 在屏幕中查找指定颜色 |
| `颜色存在检测` | 检测指定颜色是否存在于屏幕 |
| `颜色比较` | 比较两个颜色是否相同 |
| `点击颜色` | 点击屏幕上找到的颜色位置 |

### 文本处理类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `OCR文字识别` | 识别屏幕上的文字 |
| `OCR查找文字` | 在屏幕上查找指定文字 |
| `OCR文字存在检测` | 检测指定文字是否存在于屏幕 |
| `文本拼接` | 将多个文本拼接在一起 |
| `文本截取` | 截取文本的指定部分 |
| `正则匹配` | 使用正则表达式匹配文本 |
| `文本分割` | 按指定分隔符分割文本 |
| `文本比较` | 比较两个文本是否相同 |
| `文本替换` | 替换文本中的指定内容 |
| `JSON解析` | 解析 JSON 格式数据 |
| `生成JSON` | 将数据转换为 JSON 格式 |
| `文本编码/解码` | 对文本进行编码或解码 |
| `文本加密` | 对文本进行加密处理 |

### 数值计算类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `数值加减乘除` | 基本的数学运算 |
| `取模运算` | 计算余数 |
| `绝对值` | 计算数值的绝对值 |
| `数值比较` | 比较两个数值的大小 |
| `随机数生成` | 生成指定范围的随机数 |
| `数值取整` | 将数值转换为整数 |
| `数学表达式` | 计算复杂的数学表达式 |

### 数据结构类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `列表操作` | 创建、添加、删除、获取列表元素 |
| `字典操作` | 创建、添加、删除、获取字典键值对 |
| `变量操作` | 设置和获取变量值 |

### 应用管理类动作
| 动作类型 | 功能说明 |
|---------|---------|
| `打开应用` | 启动指定应用 |
| `打开URI` | 通过 URI 打开应用或页面 |
| `打开快捷方式` | 启动应用快捷方式 |
| `获取当前应用` | 获取当前前台运行的应用 |
| `等待应用` | 等待指定应用进入前台 |

## 其他功能特性

- **屏幕录制**：记录屏幕操作并生成自动化脚本
- **OCR 识别**：通过集成 OCR 服务实现文字识别
- **无障碍服务**：基于 Android 无障碍服务实现自动化控制
- **定时任务**：支持定时执行自动化任务
- **手势模拟**：支持复杂手势操作的模拟
- **悬浮窗控制**：提供悬浮窗界面快速控制
- **任务复用**：支持子任务调用，实现任务模块化
- **变量系统**：支持全局和局部变量，实现数据传递
- **标签管理**：支持任务和变量的标签分类

## 技术栈

- **语言**：Java + C++ (NDK)
- **构建工具**：Gradle (Kotlin DSL)
- **最低支持版本**：Android API 24 (Android 7.0)
- **目标版本**：Android API 34
- **主要依赖库**：
  - AndroidX AppCompat
  - Material Design
  - Navigation Component
  - MMKV (腾讯键值存储)
  - Gson (JSON 处理)
  - Shizuku (系统级权限管理)
  - LiteRT (OCR 文字识别)
  - ZXing Lite (二维码扫描)
  - FlexboxLayout (流式布局)
  - exp4j (数学表达式计算)
  - TinyPinyin (中文拼音转换)
  - HiddenApiBypass (绕过隐藏API限制)

## 许可证

本项目使用 [GNU General Public License v3.0](LICENSE) 许可证。

## 贡献指南

欢迎提交 Issue 和 Pull Request 来改进项目。

## 联系方式

如有问题或建议，请通过以下方式联系：
- 提交 GitHub Issue
- 发送邮件至项目维护者

## 致谢

感谢所有为本项目做出贡献的开发者和用户。
